/* =====================================================================
   Parole.example.sql — ȘABLON (fără parole reale, poate fi în Git)

   Copiați acest fișier ca "Parole.local.sql" în același folder și
   completați valorile. Parole.local.sql este în .gitignore.
   Evitați în parole caracterele  ; ' " $ ( ) \  (creează probleme în
   SQLCMD / connection string). Minim 12 caractere, cu litere mari, mici,
   cifre și simboluri (CHECK_POLICY = ON).
   ===================================================================== */

-- Conturi tehnice folosite de aplicație (08_Security.sql)
:setvar ParolaApp "<completeaza>"
:setvar ParolaRapoarte "<completeaza>"

-- Login-uri SQL personale (12_TeamAccounts.sql)
:setvar ParolaSqlDmitri "<completeaza>"
:setvar ParolaSqlSergiu "<completeaza>"
:setvar ParolaSqlAlexandr "<completeaza>"
:setvar ParolaSqlMaxim "<completeaza>"

-- Conturi personale în aplicație (12_TeamAccounts.sql)
:setvar ParolaAppDmitri "<completeaza>"
:setvar ParolaAppSergiu "<completeaza>"
:setvar ParolaAppAlexandr "<completeaza>"
:setvar ParolaAppMaxim "<completeaza>"
